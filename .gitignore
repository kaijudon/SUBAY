# Environments
.venv/
__pycache__/
*.pyc

# Secrets — supplied by a root-owned 0600 systemd env file in production, never committed.
.env

# Local dev database
*.sqlite3

# Frozen de-identified exports — regenerated by export_analysis_set, never tracked.
analysis_sets/

# Raw genotyping artifacts (.ab1) written under MEDIA_ROOT by ingest_genotyping — never tracked.
media/

# Tooling
.pytest_cache/
.claude/
.ralph/
node_modules/

# Ralph per-slice plan files — local-only working notes, regenerated by the Planner hat.
docs/plans/
cmv_grilling_session.md
logs/
.worktrees/

# Ralph orchestrator configs — superseded by sandcastle, slated for Phase E removal.
hats.yml
ralph.yml

generate_OTP.py

# --- Non-skeletal scaffolding: keep the public repo to the app skeleton only ---
# The build/orchestration harness, agent skills, planning specs, teaching notes,
# and generated artefacts stay local; only the Django app (subay/, templates/,
# static/, deploy/, manage.py, deps, README) ships.
.agents/
.sandcastle/
.worktrees/

# Agent/process instruction + spec docs (local working material).
AGENTS.md
CLAUDE.md
MISSION.md
NOTES.md
skills-lock.json
*_prompt.md
Slice[0-9]*.md

# Study specs and reference material (source-of-truth lives outside the code repo).
prd/
reference/
docs/decisions/
docs/2026-06-11-slice0-walking-skeleton-design.md

# Brand/course assets (masters live outside the code repo).
Assets/
assets/

# One-off scripts / orchestration runners (not part of the app).
scripts/

# collectstatic output — regenerated from static/ + app static dirs, never tracked.
staticfiles/
