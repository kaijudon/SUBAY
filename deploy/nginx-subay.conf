# SUBAY — localhost-only HTTPS reverse proxy to the gunicorn Unix socket.
# The app listens on no public interface: nginx binds 127.0.0.1 only.
upstream subay_app {
    server unix:/run/subay/gunicorn.sock;
}

server {
    listen 127.0.0.1:443 ssl;
    server_name localhost;

    ssl_certificate     /etc/subay/tls/subay.crt;   # locally-trusted cert
    ssl_certificate_key /etc/subay/tls/subay.key;
    ssl_protocols       TLSv1.2 TLSv1.3;

    access_log /var/log/nginx/subay.access.log;
    error_log  /var/log/nginx/subay.error.log;

    client_max_body_size 25m;   # headroom for .ab1 / report uploads in later slices

    location /static/ {
        alias /opt/subay/staticfiles/;
    }

    location / {
        proxy_pass         http://subay_app;
        proxy_set_header   Host              $host;
        proxy_set_header   X-Forwarded-Proto $scheme;
        proxy_set_header   X-Real-IP         $remote_addr;
    }
}
