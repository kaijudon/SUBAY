# deploy/systemd/subay-backup.timer — Slice 15 §5: run the backup nightly.
[Unit]
Description=Nightly SUBAY backup

[Timer]
# 02:30 local time; Persistent catches up a missed run after the box was off.
OnCalendar=*-*-* 02:30:00
Persistent=true

[Install]
WantedBy=timers.target
